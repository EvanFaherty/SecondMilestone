<h2> All about {{person.name}}</h2>
<a href=/{{this.name}}>
      {{this.name}}</a></li>

<h3>Name {{person.name}}</h3>

{{#if person.dob}}
<h3>Date of Birth {{person.dob}}</h3>
{{/if}}

{{#if person.dob}}
<h3>Goals {{person.goals}}</h3>
{{/if}}

<img src={{person.imageurl}} alt="photo of the person" height="280" width="250">
